# 每日更新说明

## 两个快捷文件的区别

- `启动预览.cmd`：只查看网站当前内容，不去重、不打标、不更新数据。
- `每日更新网站.cmd`：在影刀完成后，执行去重、AI 打标、同步 React 数据、生成压缩图片、构建网站，并启动预览。

## 每天的正确顺序

1. 在影刀中完成爬图。
2. 让影刀把图片保存到 `lgk/public/image`。
3. 让影刀把素材表保存或覆盖为 `lgk/灵感库.xlsx`。
4. 等影刀与 Excel 完全结束写入，避免图片仍在下载或 Excel 文件仍被占用。
5. 双击 `每日更新网站.cmd`。
6. 看到“完成”并打开网站后，检查新图片、标题和标签。

一键脚本会依次执行：

1. 按 SHA-256 删除内容完全相同的新副本；不会合并仅仅相似的图片。
2. 从 Excel 按文件名匹配标题、作者、点赞和原始网址。
3. 使用 qwen3-vl-plus 为新增或内容变更的图片打标；已有图片使用缓存，不重复计费。
4. 更新 `src/data.json`。
5. 生成响应式 WebP 图片和 `src/image-manifest.json`。
6. 生成最新 `dist` 网站构建文件并做完整性检查。
7. 启动本地预览。

在迁移期间，如果影刀仍然把 Excel 写到外层 `灵感库/灵感库.xlsx`，脚本会检测其修改时间：外层文件较新时自动复制到 `lgk`。外层旧文件删除后，这段兼容逻辑会自动跳过。长期仍建议直接把影刀的 Excel 输出改到 `lgk/灵感库.xlsx`。

每次运行的信息保存在 `update-log.txt`。如果更新失败，窗口会停留并显示原因，不会再直接闪退。更新完成后，预览会在独立窗口中运行；再次更新不会被已打开的预览或日志占用。

## API Key

API Key 已放在 `.private/API KEY.txt`。这个目录已列入 `.gitignore`，使用 Git 提交时不会进入仓库。

不要把 `.private` 文件夹拖到 GitHub 网页上传界面，也不要删除 `.gitignore`。如果密钥曾被提交到公开仓库，应立即在阿里云控制台撤销并更换。

## 上传网站更新时应提交什么

通常提交这些变化：

- `public/image` 中的新原图或去重结果
- `public/optimized` 中生成的响应式图片
- `src/data.json`
- `src/image-manifest.json`
- `ai-tags.json`、`dedup-report.json`、`tagging-report.json`

不要提交：

- `.private/`
- `灵感库.xlsx`
- `node_modules/`
- `dist/`，除非你的托管方式明确要求提交构建产物

## 常见失败

- 提示 Excel 缺失：检查文件是否名为 `灵感库.xlsx`，并位于 lgk 根目录。
- 提示图片目录缺失：检查影刀输出是否为 `lgk/public/image`。
- 提示 API Key 缺失：检查 `.private/API KEY.txt`。
- AI 接口报错：检查密钥、URL、模型权限和账户额度，修复后重新运行；已经成功的标签会保留缓存。
- 图片在更新期间变化：说明影刀仍在写文件，等待爬取完成后重试。
- 找不到 Python 或 npm：安装 Python 3 和 Node.js 22，然后重新运行。
