# 网站性能审核

审核日期：2026-09-12。当前网站为 React + Vite 静态项目，保留 FoldText、TargetCursor、GlowCursor、SpecularButton 和 Stack 动效。

## 已完成的优化

- 18 张瀑布流图片生成 WebP 响应式版本，提供 480 / 960 / 1440 像素宽度档位，不放大原图。
- 瀑布流小图总量从约 4.65 MB 降至约 0.72 MB，减少约 84.5%。完整原图继续保留，用于详情和下载。
- 图片尺寸和横竖比例在构建前生成，首页不再为了读取尺寸而下载全部原图。
- 瀑布流接近可视区域后再挂载，并使用浏览器原生懒加载。
- 首页正面图优先加载；背面卡片先加载小图，翻到正面后按需加载更清晰版本。
- 鼠标光迹、按钮高光和锁定光标在空闲、离开可视区域或页面进入后台后暂停。
- 鼠标首次移动后才加载光标动效模块；关闭详情弹窗时释放弹窗中的图形资源。
- 文字动效使用 GSAP，滚动触发改为浏览器原生 IntersectionObserver，移除了未使用的 ScrollTrigger。
- JavaScript 已分包，解决原先单个约 572 KB 文件的问题。当前最大单个脚本约 222 KB。

## 本机生产版本样本

测试环境为本机 Chromium、1700 × 1000 PC 视口、Vite 生产构建预览，没有进行网络限速。这些数据用于前后对比，不代表真实公网访问速度。

| 指标 | 优化前 | 优化后 |
|---|---:|---:|
| 首次内容绘制 FCP | 492 ms | 448 ms |
| 最大内容绘制 LCP | 760 ms | 448 ms |
| 页面 load 事件 | 706 ms | 396 ms |
| 首屏资源响应体合计 | 4.84 MB | 0.57 MB |
| 首屏请求数 | 22 | 13 |
| 动画帧间隔 P95 | 16.8 ms | 16.8 ms |

优化前无人操作时，WebGL 仍约每秒绘制 60 次。优化后完成入场绘制后会停止，鼠标移动时恢复。

## 注意事项

- 原图约 4.65 MB，响应式 WebP 全部档位约 5.68 MB。项目磁盘体积增加，是为了同时保留原图下载和多尺寸快速展示。
- 评论和收藏保存在访问者当前浏览器，不会跨设备同步。
- 本地数据见 `performance-results.json`；它是一次代表性样本，不是 Lighthouse 或真实用户 Core Web Vitals 报告。
- 后续上线后，建议再从真实域名测试桌面端和移动端的 LCP、INP、CLS。

## 已验证

- `npm ci` 可在独立干净目录安装依赖。
- `npm run build` 构建成功。
- `npm run check` 检查 18 张原图和全部响应式图片、缓存文件、路径和单文件体积。
- `npm audit` 未发现当前依赖数据库中的已知漏洞。
- 首页、图集切换、瀑布流、搜索筛选、详情、收藏和评论发布/删除均可运行。
